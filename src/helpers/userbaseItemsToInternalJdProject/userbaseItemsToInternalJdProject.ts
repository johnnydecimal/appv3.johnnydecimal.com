// === Types    ===-===-===-===-===-===-===-===-===-===-===-===-===-===-===-===
// import {
//   JDProjectNumbers,
//   JDAreaNumbers,
//   JDCategoryNumbers,
//   JDIdNumbers,
// } from "@types";

import { InternalJDSystem, UserbaseItem } from "@types";

export const userbaseItemsToInternalJdProject = (
  userbaseItems: UserbaseItem[]
): InternalJDSystem => {
  // Okay, crib all this from scratch.tsx
};
