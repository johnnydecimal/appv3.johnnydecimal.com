import { insert } from "../insert/insert";

it("should be a placeholder", () => {
  expect(true).toBeTruthy();
});
// it("should detect proacid type from input", () => {

// })

// it("should insert", () => {
//   expect(insert); // nah doofus, check the proacidDetector first
// });
